name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1  # Add this line

flutter:
  uses-material-design: true

flutter_icons:  # Add this configuration section for app icons
  android: true
  ios: true
  image_path: "assets/icon/icon.png"

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # Fonts section for custom fonts (optional - if you want to use asset fonts)
  # fonts:
  #   - family: SFPro
  #     fonts:
  #       - asset: fonts/SF-Pro-Display-Light.otf
  #         weight: 300
  #       - asset: fonts/SF-Pro-Display-Regular.otf
  #         weight: 400
  #   - family: Roboto
  #     fonts:
  #       - asset: fonts/Roboto-Regular.ttf
  #         weight: 400